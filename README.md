# pi-cline

Cline/ClinePass OAuth provider extension for pi.

## Development

Install the development dependencies, then install the Changelogger git hooks:

```sh
npm install
npx changelogger install
```
